import { makeSourcerer } from '../base';
import { sourceRanks } from '../ranks';

export const flixhq = makeSourcerer({
  id: 'flixhq',
  name: 'FlixHQ',
  rank: sourceRanks.FLIXHQ,
  // ... existing code ...
}); 
